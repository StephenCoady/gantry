'use strict';

var assert = require('assert');

describe('Dummy Tests to ensure Travis is configured properly', function () {
  it('2 equals 2!', function () {
    assert(2==2, '2 should be equal 2!');
  });
  
  it('3 equals 3!', function () {
    assert(3==3, '3 should be equal 3!');
  });
});
